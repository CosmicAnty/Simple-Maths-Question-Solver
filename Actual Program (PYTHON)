question = 'What is the sum of seven and seven?'
# Question with words for numbers
question2 = 'What is the sum of 3 and 3?'
# Question with integer numbers

numbers_word = [
    'zero',
    'one',
    'two',
    'three',
    'four',
    'five',
    'six',
    'seven',
    'eight',
    'nine'
    ]

answer = 0
integer = 0
question_list = question.split()
for text in numbers_word:
    for word in question_list:
      if str(text) in word:
        answer += integer
    integer += 1
    
if answer != 0:
    print(answer)

answer2 = 0
integer2 = 0
numbers_range = range(10)
question_list2 = question2.split()
for word in question_list2:
   for integer2 in numbers_range:
      if str(integer2) in word:
        answer2 += integer2

if answer2 != 0:
    print(answer2)
